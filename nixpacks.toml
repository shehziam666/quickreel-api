[phases.setup]
nixPkgs = ["nodejs_18", "python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["npm install", "python3.11 -m pip install yt-dlp"]

[start]
cmd = "node api/download.js"
